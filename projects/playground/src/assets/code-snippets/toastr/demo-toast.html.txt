<div class="notification" [ngClass]="data.type">
    <button class="notification-close-button" (click)="dismiss()" aria-label="Close toast">
        ✕
    </button>

    <div class="notification-content">
        <h4 class="notification-title">{{ data.title }}</h4>
        <p class="notification-message">{{ data.message }}</p>
    </div>
</div>