.button-container {
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    gap: 1rem;

    & button {
        cursor: pointer;
    }
}

.confirm-button {
    padding: 12px 24px;
    font-size: 16px;
    cursor: pointer;

    background-color: var(--bx-success-bg);
    color: var(--bx-success-text);
    border: none;
    border-radius: 4px;
}

.cancel-button {
    padding: 12px 24px;
    font-size: 16px;
    cursor: pointer;

    background-color: var(--bx-bg-danger);
    color: var(--bx-text-danger);
    border: none;
    border-radius: 4px;
}