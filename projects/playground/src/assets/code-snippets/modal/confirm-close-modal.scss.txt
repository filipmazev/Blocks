.button-container {
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    gap: 1rem;

    & button {
        cursor: pointer;
    }
}

.confirm-button {
    padding: 12px 24px;
    font-size: 16px;
    cursor: pointer;

    background-color: var(--fm-button-confirm);
    color: var(--fm-text-confirm);
    border: none;
    border-radius: 4px;
}

.cancel-button {
    padding: 12px 24px;
    font-size: 16px;
    cursor: pointer;

    background-color: var(--fm-button-warn);
    color: var(--fm-text-warn);
    border: none;
    border-radius: 4px;
}