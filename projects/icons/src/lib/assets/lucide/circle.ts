import { defineIcon } from '../../helpers/define-icon';

export const lucideCircle = defineIcon({
  name: 'circle',
  viewBox: '0 0 24 24',
  svgContent: `<circle cx="12" cy="12" r="10" />`
} as const);
